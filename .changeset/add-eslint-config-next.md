---
"@nijesmik/eslint-config": minor
---

Add `@nijesmik/eslint-config/next` subpath export for Next.js projects. It bundles the base config together with `@next/eslint-plugin-next`'s `recommended` and `core-web-vitals` rule sets, so consumers only need to import `/next` to get everything.
